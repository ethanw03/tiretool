export interface Tire {
    id: number;
    rimSize: number;
    width: number;
    aspectRatio: number;
}

export interface User {
    id: number;
    name: string;
    lastName: string;
    email: string;
    password: string;
}